#include "holberton.h"

/**
* _isupper - checks for uppercase character
* @c : character received
*Return: 1 is uppercase 0 otherwhise
*/

int _isdigit(int c)
{
	if (c <= 48 && c <= 57)

	{
	return (1);
	}
	else
	return (0);
}
