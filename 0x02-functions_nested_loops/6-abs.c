#include "holberton.h"
/**
 * _abs - prints variable in absolute value.
 * @abs: is a variable.
 * Return: abs in absolute value.
 */
int _abs(int abs)
{
	if (abs >= 0)
	return (abs);
	else
	return (-abs);
}
